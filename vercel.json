{
    "version": 2,
    "builds": [
        {
            "src": "backend/app.py",
            "use": "@vercel/python"
        },
        {
            "src": "frontend/**",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "backend/app.py"
        },
        {
            "src": "/(.*)",
            "dest": "backend/app.py"
        }
    ],
    "installCommand": "pip install -r requirements.txt",
    "env": {
        "PYTHON_VERSION": "3.11"
    }
} 